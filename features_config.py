# Define the features system will expect
features = [
    "typing_speed",        # float
    "click_rate",          # float
    "mouse_speed",         # float
    "scroll_ratio",        # float
    "ip_country",          # categorical (e.g., "IN", "US")
    "device_type"          # categorical (e.g., "Laptop", "Mobile")
]
